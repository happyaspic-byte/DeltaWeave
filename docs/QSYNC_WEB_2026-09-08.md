# qSync 웹 구현 기록

작성일: 2026-09-09
브랜치: `feat/qsync-web-20260908`
기준: B 계약·retry API 통합과 C 최신 checkpoint `b2317f6`

이 변경은 기존 한국어 동기화 관리 화면과 수동 폴더 연결을 유지하면서
키 기반 폴더 공유 흐름을 추가한다. overview/folders의 기본 동작은 `폴더
공유`와 `키로 연결`이며, 기존 `폴더 추가`는 `고급 연결` 안에서 계속
사용한다. 화면은 owner의 공유·pending 목록을 실제 snapshot 데이터로
표시하고, 등록된 멤버 수를 온라인 수로 추정하지 않는다. `active_peer_count`
와 현재 연결 장치를 별도 표시하며 전송량은 bytes, 속도는 bytes/sec이다.

## 계약과 경로

웹 API는 `/api/v1/shares` 이하의 B Manager 경로를 사용한다.

- 키 body의 `key`는 route에서 Manager 입력 `encoded_key`로 전달한다.
- share/invitation ID는 URL에서 percent-encode하고, 서버는 32바이트 소문자
  hex 64자로만 받는다. `member_id`와 `request_id`는 불투명 문자열 경계를
  적용한다.
- `preview`는 로컬 서명·만료를 확인하고, `validate`는 owner 온라인 발급
  기록을 확인한다. 사용자는 한 번의 `키 확인` 동작으로 두 단계를 거친다.
  offline이면 확인되지 않은 상태를 명시한 뒤 폴더 선택과 pending join을
  허용한다. invalid/revoked 키는 join하지 않는다.
- 가입은 이 웹 콘솔을 실행하는 장치의 authenticated `/browse`에서 고른
  서버 폴더를 사용한다. API 응답 유실 재시도는 입력 fingerprint가 같을 때
  같은 `request_id`를 재사용하며 입력이 바뀌면 새 ID를 만든다. 세대와
  mounted guard가 늦은 응답이 현재 화면을 덮지 않게 한다.
- 오류는 `error_code`의 고정된 한국어 문구만 화면에 보여준다. 내부 오류
  chain, 경로, ID 전체, bearer는 사용자 오류·activity·URL·SSE에 넣지 않는다.

## 비밀과 상태 경계

발급된 raw key는 발급 응답에서 한 번만 표시하고 React memory에만 둔다.
복사 뒤 목록을 다시 조회해 키를 재노출하지 않는다. modal close, 성공,
취소, 만료, logout 시 상태와 operation tracker를 지운다. localStorage,
sessionStorage, URL, console, SSE에는 키를 쓰지 않으며 스크린샷 검증 시
`data-sensitive="share-key"` 영역을 마스킹한다. 만료 timer는 브라우저의
약 24.8일 한계를 넘지 않도록 제한된 재예약 후 실제 Unix seconds를 다시
확인한다. 관리형 timestamp는 Unix seconds이므로 표시할 때만 milliseconds로
변환한다.

상태 값은 계약의 8개(`waiting`, `offline`, `initial_sync`, `complete`,
`conflict`, `revoked`, `error`, `paused`)를 그대로 표시한다. `pending`은
가입·멤버 철회의 진행 상태로, 결과의 `completion`이 명시적으로
`complete`일 때만 완료로 표시한다. member 철회가 pending이면 “연결 종료
확인 중”으로 남기고 재조회/동일 요청 재시도를 안내한다. 네트워크 분할에서
owner handler drain만으로 전역 전송 종료를 주장하지 않는다. key 철회는
신규 가입을 막는 즉시 완료 경계이며 이미 전달된 파일을 회수하지 않는다.
공유 연결 해제는 managed 연결·등록만 정리하고 로컬 파일과 복구 데이터는
유지한다.

## 담당 파일과 완료 조건

웹 UI/API/테스트는 `web/src/App.tsx`, `api.ts`, `types.ts`, `share.tsx`,
`share.test.tsx`, `components.tsx`, `styles.css`에 있다. HTTP adapter와
경계 테스트는
`crates/deltaweave-web/src/{share_routes.rs,routes.rs,routes/tests.rs}`가
담당한다. 새 route는 기존 protect middleware 아래에 있으며 login, Host,
same-origin Origin, Sec-Fetch, CSRF, 64 KiB 제한을 그대로 통과해야 한다.

완료 판정에는 다음이 모두 필요하다.

1. B Manager의 실제 메서드와 error classifier가 web worktree에 연결되어
   `DELTAWEAVE_REQUIRE_WEB_ASSETS=1 cargo test/clippy -p deltaweave-web`
   가 통과한다. retry route도 child process의 실제 Manager와 HTTP adapter를
   통해 owner 복구·pending 재시도를 확인해야 한다.
2. 아래 npm 검사와 API/UI 경계 테스트가 통과한다.
3. 실제 browser에서 1440/390 viewport, Tab/Escape/focus 복귀, long key,
   clipboard 실패, console/pageerror 및 masked screenshot을 확인한다.
4. F 단계에서 분리된 owner/RO/RW 프로세스, Windows native, Internet/N0
   전송과 실제 worker join/resume를 검증한다. 로컬 프로세스 수를 물리적
   3기기 증거로 세지 않는다.

## 현재 증거

실행 로그는 `/home/ubuntu/project/DeltaWeave-qsync-evidence/2026-09-08/c-web/`
와 `/home/ubuntu/project/DeltaWeave-qsync-evidence/2026-09-09/c-web/`에
보관한다. 2026-09-08 21:41:54Z에 별도 owner/member 프로세스로
create → issue → preview → validate → join을 실행했다. create는 201,
issue/preview/validate/join은 200이었고 join 결과는 `enrolled`/`initial_sync`였다.
발급 키 목록에는 raw key가 없었으며, source와 destination 파일의 SHA-256은
각각 `408ba62b60e58474307dfb671e9bf523f45d4830f499adeb5f075907c975fd31`로
일치했다. 원문은 `actual-api-browser-final-20260908-private.log`와
`browser-join-file-hash-focus-final-20260908.log`에 있으며 키와 인증값은
기록하지 않는다.

| 명령 | 결과 |
| --- | --- |
| `npm --prefix web ci` | exit 0, package 166개, 취약점 0 |
| `npm test -- --run` (focus 수정 후) | exit 0, 5 files / 44 tests passed |
| `npm run build` (focus 수정 후) | exit 0, Vite 4574 modules |
| `DELTAWEAVE_REQUIRE_WEB_ASSETS=1 cargo check --locked -p deltaweave-web --all-targets --all-features` | exit 0 |
| `cargo test --locked -p deltaweave-web --all-targets --all-features` | exit 0, unit 14 + integration 15 = 29 passed; child HOME/TMPDIR 격리 (2026-09-09 00:06:38Z) |
| `cargo clippy --locked -p deltaweave-web --all-targets --all-features -- -D warnings` | exit 0 |
| `npm test -- --run src/behavior.test.tsx` (nested/focus) | exit 0, 13 tests passed |
| `cargo build --locked -p deltaweave --bin deltaweave` with web assets | exit 0, binary SHA-256 `fc4f5577dd2d6845caa9548bfd2e56d58102c2e5f584c3129de0e83c1a36e9b9` |
| private helper Windows native ACL execution | exit 0, 2 tests passed on approved Windows Server 20348 |

1440px와 390px에서 managed board가 기본 화면으로 보이고 `scrollWidth`가
각각 viewport와 같았으며 상단 action이 모두 화면 안에 있었다. 두 크기에서
axe 위반, console line, pageerror는 0이었다. 최신 redacted 캡처는
`browser-owner-1440-latest-final-redacted.png`와
`browser-owner-390-latest-final-redacted.png`이다. 390px member 화면도
managed board와 no-overflow를 확인했고, 키 modal 종료 뒤 sensitive textarea와
값이 DOM에서 제거됐다(`browser-member-390-latest-focus-redacted.png`).
실제 RO 가입 결과 화면은 `browser-member-join-390-final-redacted.png`로
별도 보관했으며, 양성 API 가입·파일 hash 결과와 함께 해석한다.
키 modal 열기 시 textarea로 focus되고 Escape 뒤 `키로 연결` trigger로
복귀하는 실제 Chromium 검증은 `browser-focus-escape-latest-20260908.log`에
있다. StrictMode와 부모 dialog 안의 중첩 close 복귀는 behavior 테스트에
포함한다.

추가로 인증된 pending retry route는
`/home/ubuntu/project/DeltaWeave-qsync-evidence/2026-09-09/c-web/http-pending-retry-route-isolated-real-20260909.log`에서
child test 1/1로 확인한다. owner 재기동 뒤 원래 request ID를 사용한
wrong-share 409, retry 200 `enrolled`, 동일 요청 replay 200·동일 member ID를
확인한다. 만료된 pending의 동작은 B Manager acceptance에서 검증하고,
route의 `pending_expired` 오류 분류와 HTTP 422 매핑은 web adapter 경계에서
다룬다. route 테스트는 짧은 bearer TTL에 의존하지 않고 durable waiting/retry
경계에 집중한다. 처음 full all-targets 실행에서 legacy API receiver 2개가
`phase=idle`, `activity_status=error`, 고정 `error_class=other`로 대기 만료된
원인은 outer HOME에 남은 managed admission marker와 삭제된 TempDir의
불일치로 확인했다. route fixture를 전용 child HOME/TMPDIR로 실행한 뒤 같은
명령이 29/29 통과했다. 진단은 raw activity error를 보존하지 않고
`phase`, `activity_status`, 고정 `error_class`만 남긴다. 초기 실패와 수정 후
실행 로그는 각각
`/home/ubuntu/project/DeltaWeave-qsync-evidence/2026-09-08/c-web/cargo-web-focused-final-20260908.log`
및 `/home/ubuntu/project/DeltaWeave-qsync-evidence/2026-09-09/c-web/cargo-web-focused-isolated-route-20260909.log`에 보관한다.

CI `workflow_dispatch` run `34293135007`은 integration branch의
`fea9b3ace610800a145fd5f8e82d409cda364acc`에서 실행되었다. `Linux quality
gates`는 Tests 단계에서 `scans_real_files_and_reuses_index_across_receiver_mode`,
`syncs_two_real_peers_and_denies_unlisted_identity`가 각각
`wait_for_receiving` 10초 경계에서 실패했다(13 pass/2 fail, 10.14초).
이 SHA에는 child HOME 격리 커밋 `b2317f6`이 아직 포함되지 않았으므로,
fixture가 남긴 managed admission marker 오염을 해결한 현재 C 재실행 결과와
분리한다. 이는 B managed acceptance 실패가 아니다. `Windows tests`는
`private::tests::native_acl_preparation_is_idempotent_and_fails_closed`와
`managed_error_tests::pending_preengine_failure_reattaches_the_exact_admission_lease`
에서 초기 managed private 준비의 `PermissionDenied`로 중단되었다(11 pass/2
fail, 6.41초); watcher 최종 회귀 증거로 세지 않는다. `qsync-native-artifact /
Windows native release artifact`는 성공했다. 생성된 `deltaweave.exe`는
`x86_64-pc-windows-msvc`, 33,272,320 bytes,
SHA-256 `7a8bd8c84ca625a2ad5e2a947551270009395c4cc1d5219b1b1070380003bfe7`이며,
manifest의 source SHA와 workflow SHA는 위 run SHA와 같다. 전체 F 완료를 뜻하지
않는다.

각 결과의 원문은 evidence 디렉터리의 `npm-ci-after-recovery-20260908.log`,
`npm-test-focus-final-20260908.log`, `npm-build-focus-final-20260908.log`,
`cargo-check-web-after-01a96b6.log`,
`cargo-test-web-isolated-home-01a96b6-rerun.log`,
`cargo-clippy-web-isolated-route-box.log` 등에 있다. Windows ACL helper의
native pass는 helper 경계만 증명한다. owner/member는 한 Linux 호스트의
격리 프로세스이므로 물리 3기기, Windows 전체 앱, Internet/N0 relay,
다중 공급자 또는 F 단계 worker join/resume 증거로 확대하지 않는다. B의
후속 lease/lifecycle/clock rollback 변경과 D1 classifier 변경은 root가
별도로 통합·재검증해야 한다. 현재 CI reusable caller는 `ci.yml`의
`workflow_dispatch` 전용 job이며, 호출되는 `qsync-native-artifact.yml`의
`workflow_call` 구현은 integration의 `01d01cb`에서 제공된다.
