# QSync CI 검증 기록 — 2026-09-09

## 기준

- GitHub Actions CI run: [34311827636](https://github.com/happyaspic-byte/DeltaWeave/actions/runs/34311827636)
- 실행 source SHA: 2f44d9fbfbe1c4779bd59f78fe9cc4ff27f41cd6
- 실행 결과: success
- outer 집계 규칙: Cargo 결과를 실행한 27개 outer 바이너리만 집계하고, filtered 결과와 admission 테스트가 DW_SYNC_ADMISSION_CHILD=1로 자기 바이너리를 재실행한 child 결과를 제외했다.

이 기록은 위 integration source의 CI 검증 결과다. 기본 브랜치 반영이나 제품 배포를 의미하지 않는다.

## CI 결과

| job | 결과 | outer 집계 | 범위 |
| --- | --- | ---: | --- |
| Linux quality gates | success | 400 passed / 0 failed (27 outer 바이너리) | 웹 자산·Vitest·build, fmt, Clippy, workspace all-targets/all-features test, CLI self-test, rustdoc, media/link 검사, release build |
| Windows tests | success | 367 passed / 0 failed (27 outer 바이너리) | 웹 자산·Vitest·build, workspace all-targets/all-features test, CLI self-test |

주요 명령은 다음과 같다.

- 웹: npm --prefix web ci; npm --prefix web test; npm --prefix web run build
- Linux 품질: cargo fmt --all -- --check; cargo clippy --locked --workspace --all-targets --all-features -- -D warnings; cargo test --locked --workspace --all-targets --all-features --no-fail-fast
- 실행·문서·빌드: cargo run --locked -p deltaweave -- self-test; cargo doc --locked --workspace --no-deps --all-features; cargo build --locked --workspace --release --all-features
- Windows 테스트: cargo test --locked --workspace --all-targets --all-features --no-fail-fast; cargo run --locked -p deltaweave -- self-test
- 모든 job은 DELTAWEAVE_REQUIRE_WEB_ASSETS=1 조건을 유지했다.

## 별도 증거

ACL probe job은 성공했지만 workspace test 집계와 별도다. 관리형 private ACL 경계의 fail-closed probe만 검증한다.

native Windows artifact job도 별도 성공했다. manifest의 source SHA와 workflow SHA는 모두 2f44d9fbfbe1c4779bd59f78fe9cc4ff27f41cd6이며, artifact 실행 파일의 SHA-256은 186b9e177d8b3a8246ceb994d00402b827fb8481b26642edf4ce62983988453a, 크기는 33488896 bytes다. web install, web build, native build, Cargo.lock 확인, self-test가 각각 exit 0이고 self-test status는 pass였다. 이 artifact는 CI provenance 증거이며 실제 Windows 서버 동기화 증거가 아니다.

## 이전 실패와 수정

직전 CI run 34309126401에서는 Linux에서 remove_tombstone_replay_after_recovery_is_idempotent가 redb_table_missing 분류로 실패했다. 새 registry 초기화에서 share roster heartbeat 테이블이 열리지 않은 경로가 원인이었다.

같은 run의 Windows에서는 다섯 개 logical failure가 있었다. managed observer가 완료 상태를 다음 scanning 상태로 덮는 관찰 경합, managed private ACL 준비를 우회한 fixture의 fail-closed 오류, 그리고 normal 경로와 Windows extended canonical 경로의 예상값 불일치가 확인됐다. roster heartbeat 초기화 오류도 양쪽 환경에서 중복 관찰됐지만 하나의 logical failure로 셌다.

후속 수정 checkpoint는 00b262a와 99fb863이며, 현재 source에는 registry 초기화·managed recovery/observer 검증·private admission canonical expectation 보정이 통합됐다. 이후 CI 34311827636에서 위 logical failure들은 재현되지 않았다. 오류 원문이나 비밀·ticket·endpoint 값은 이 문서에 보존하지 않는다.

## 범위와 남은 검증

이번 CI 성공은 D3의 실제 N0 lookup 및 relay payload, E의 완성된 share-swarm consumer/provider 전송, F의 실제 owner/RW/RO 세 호스트 흐름을 증명하지 않는다. 브라우저 real-key join, 양방향 RW 변경과 삭제 충돌, RO 보존, response-loss/restart/address-change, revoke·pause·resume·security matrix도 별도 실행 항목이다. 기본 브랜치 main의 반영·push 일치도 이 기록의 범위가 아니다.
